- Clique droit sur le programme et fait édtier avec NotePad où un autre outil
- Changer simplement les 2 premiers chemins définis ainsi que donc le noms du fichier txt à traité et le fichiers à sortir

REMARQUE:python et numpy doivent être installé donc pas sur qu'ils soient utilisable :) 
- Installer python, lancer l'installateur
- Installer numpy, pip install numpy